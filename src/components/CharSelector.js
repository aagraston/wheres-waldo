import React from 'react'

function CharSelector(props) {
  const { allChars, imageClicked, checkForFound, foundChars } = props

  return <div className="drop-down-container"></div>
}

export default CharSelector
